import styles from "./ButtonSearch.module.css";

const ButtonSearch = () => {
  return (
    <button className={styles.buttonSearch} value="Search">
      Search
    </button>
  );
};

export default ButtonSearch;
